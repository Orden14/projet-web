{{ form_start(note_form) }}
    <div class="mb-3">
        {{ form_label(note_form.ressource.title) }}
        {{ form_widget(note_form.ressource.title, { attr: { class: 'form-control', placeholder: 'Titre' } }) }}
        {{ form_errors(note_form.ressource.title) }}
    </div>

    <div class="mb-3">
        {{ form_label(note_form.content) }}
        {{ form_widget(note_form.content, { attr: { class: 'form-control', placeholder: 'Votre note' } }) }}
        {{ form_errors(note_form.content) }}
    </div>

    <div class="mb-3 d-none">
        {{ form_label(note_form.ressource.description) }}
        {{ form_widget(note_form.ressource.description, { attr: { class: 'form-control', rows:3 } }) }}
        {{ form_errors(note_form.ressource.description) }}
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(note_form.ressource.parent) }}
            {{ form_widget(note_form.ressource.parent, { attr: { class: 'form-select' } }) }}
            {{ form_errors(note_form.ressource.parent) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(note_form.ressource.category) }}
            {{ form_widget(note_form.ressource.category, { attr: { class: 'form-select' } }) }}
            {{ form_errors(note_form.ressource.category) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(note_form.ressource.tags) }}
        {{ form_widget(note_form.ressource.tags, {
            attr: {
                class: 'selectpicker',
                'data-style-base': 'form-control',
                'data-width': '100%',
                'data-container': 'body',
                multiple: 'multiple'
            }
        }) }}
        {{ form_errors(note_form.ressource.tags) }}
    </div>

    <div class="form-check mb-3">
        {{ form_widget(note_form.ressource.favorite, { attr: { class: 'form-check-input' } }) }}
        {{ form_label(note_form.ressource.favorite, null, { label_attr: { class: 'form-check-label' } }) }}
        {{ form_errors(note_form.ressource.favorite) }}
    </div>

    <div class="d-grid">
        <button type="submit" class="btn btn-primary">Enregistrer la note </button>
    </div>
{{ form_end(note_form) }}
